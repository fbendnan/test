// In src/arrays.ts:

// Create an array of numbers grades with at least 3 values.

// Create an array of strings languages with "TS", "JS", "C++".

// // Create a tuple book that has: [title: string, pages: number, available: boolean].


let numbers : number[] = [12, 44, 20];
let languages : Array<string> =["TS", "JS" , "C++"];

let book : [string, number, boolean] = ["rich dad poor dad", 200, true];